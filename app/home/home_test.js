'use strict';

describe('mdp-web.home module', function() {

  beforeEach(module('mdp-web.home'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var HomeCtrl = $controller('HomeCtrl');
      expect(HomeCtrl).toBeDefined();
    }));

  });
});